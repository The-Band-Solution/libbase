---
description: Bump the project version using SemVer rules
---

To increment the project version (major, minor, or patch), run:

// turbo
1. Bump version:
```bash
python3 scripts/bump_version.py [major|minor|patch]
```
